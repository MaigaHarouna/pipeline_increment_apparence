"# pipeline_increment_apparence" 

Creation of a data pipeline to automate the historization of a SQL Server table by transferring data through a staging table before loading it into the final history table.
